fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
python-dotenv>=1.0
sse-starlette>=2.1
# Postgres (Supabase) in prod; unused when DATABASE_URL is unset (SQLite dev).
psycopg[binary,pool]>=3.1
